# Android / Gradle / IDE
*.iml
.idea
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
/build
seekdb-android-inspection/build/
/captures

# Prebuilt JNI libs (build or copy locally; do not commit binaries)
seekdb-android/src/main/jniLibs/**/*.so
